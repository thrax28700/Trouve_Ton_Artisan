{
  "name": "trouve-ton-artisan",
  "version": "1.0.0",
  "description": "Plateforme artisans Auvergne-Rhône-Alpes — monorepo",
  "private": true,
  "scripts": {
    "install:all":   "npm install --prefix backend && npm install --prefix frontend",
    "dev":           "concurrently --names \"API,WEB\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend":   "npm run dev --prefix backend",
    "dev:frontend":  "npm run dev --prefix frontend",
    "build":         "npm run install:all && npm run build --prefix frontend",
    "start":         "npm start --prefix backend",
    "start:backend": "npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
